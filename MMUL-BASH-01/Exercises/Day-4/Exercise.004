# Exercise 004 - Cycle through all files in a dir and print details

1) Write a script that cycles through files beginning with 'h' in the dir '/etc' and prints, of each file,
   - the name;
   - details (using 'ls -l' command).
