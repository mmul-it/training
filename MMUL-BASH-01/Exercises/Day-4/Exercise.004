# Exercise 004 - Cycle through all files in a dir and print details

1) Log into the machine with the credentials you own;

2) Write a script that cycles through files beginning with 'h' in the dir
   '/etc' and prints, for each file:
   - The name;
   - The details (using 'ls -l' command);
