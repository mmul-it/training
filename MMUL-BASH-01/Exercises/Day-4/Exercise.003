# Exercise 003 - Guess the passphrase!

1) Log into the machine with the credentials you own;

2) The 'read' command can be used to read the user input and save in a
   variable. The command "read input" reads a line from the standard input
   (console), putting the first word into "$input" variable.
 
   Using 'read' and an endless loop, write a script that exits only if the
   typed input matches the word 'Linux!';
