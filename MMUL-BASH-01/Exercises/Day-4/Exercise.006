# Exercise 006 - Print every element of the array

Continue the previous script:
1) Print all elements, one per line
   (remember to quote the variable "${tarantino[@]}" in for loop)

2) Sort the output ( with ... done | sort )

3) Unset element with index 3

4) Print all the indexes of $tarantino, one per line

5) Print all the elements of $tarantino with index, one per line
   <index>) <element>
