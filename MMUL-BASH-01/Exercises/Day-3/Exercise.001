# Exercise 001 - Use all the tools to solve problems

1) Log into the machine with the credentials you own;

2) Check if your machine is properly configured to access the network;

3) Look for the DNS server's IP in /etc/sysconfig/network-scripts/ifcfg-eth0 file;

4) Check if port 22 is reachable on DNS server;

5) On your machine, check if there's any TCP port in LISTEN state.
   Looking at the results, can you tell if the machine is reachable via SSH?
