# Create an ImageStream for nginx:alpine and verify available images

1) Start editing a new yaml

2) Enter the ImageStream definition which pull the nginx:alpine docker image

3) Create the ImageStream object in OpenShift

4) Show if the new ImageStream is available and look at the detail (i.e. the image provided by the stream)

5) Try listing images inside the OpenShift cluster

6) Switch to system:admin account

7) Try listing images inside the OpenShift cluster

8) Verify if the image provided by the stream are inside the OpenShift images
