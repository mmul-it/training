# Exercise 004 - Create an ImageStream for nginx:alpine and verify available images

1) Login as user "developer" and create a project named "imagestream-test";

2) Create the ImageStream definition and the relative OpenShift object which
   will pull the nginx:alpine docker image;

2) Create the ImageStream object in OpenShift;

3) Query OpenShift to check that the new ImageStream is available and look at
   the details like the image provided by the stream;

4) Try listing images inside the OpenShift cluster;

5) Switch to the admin account;

6) Try listing images inside the OpenShift cluster;

7) Verify if the image provided by the stream are inside the OpenShift images;
